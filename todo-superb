import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error('Missing Supabase environment variables');
}

export const supabase = createClient(supabaseUrl, supabaseAnonKey);

export interface Profile {
  id: string;
  name: string;
  role: 'lecturer' | 'student';
  created_at: string;
}

export interface Quiz {
  id: string;
  lecturer_id: string;
  title: string;
  description: string;
  subject: string;
  duration_minutes: number;
  total_marks: number;
  status: 'draft' | 'published';
  created_at: string;
  updated_at: string;
}

export interface Question {
  id: string;
  quiz_id: string | null;
  lecturer_id: string;
  question_text: string;
  question_type: 'mcq' | 'true_false' | 'essay';
  options: any;
  correct_answer: string;
  marks: number;
  created_at: string;
}

export interface QuizAttempt {
  id: string;
  quiz_id: string;
  student_id: string;
  started_at: string;
  submitted_at: string | null;
  score: number | null;
  status: 'in_progress' | 'submitted' | 'graded';
  created_at: string;
}

export interface StudentAnswer {
  id: string;
  attempt_id: string;
  question_id: string;
  answer_text: string;
  is_correct: boolean | null;
  marks_obtained: number | null;
  lecturer_comment: string;
  created_at: string;
}



cexejas898@keecs.com



Based on the implementation, here are the things a **Super Admin** can do in your quiz management system:

## **Current Super Admin Capabilities**

### **1. User Management**
- View all users in the system (students, lecturers, moderators, admins, other super admins)
- Search users by name, email, or index number
- Filter users by role
- Change any user's role (e.g., promote a lecturer to admin, demote an admin to lecturer)
- Delete users from the system

### **2. System Overview**
- View total user count and breakdown by role
- Monitor total quizzes and published quizzes
- Quick access to key statistics and metrics

### **3. System Settings**
- Configure site name and email
- Enable/disable student self-registration
- Require email verification for new users
- Set maximum quiz duration limits
- Enable/disable email notifications
- Put system into maintenance mode (only super admins can access)

## **Additional Super Admin Responsibilities You Could Add**

### **4. Audit & Security**
- View user activity logs
- Monitor login attempts and failed authentications
- Track suspicious activity
- View system access logs
- Manage password reset requests

### **5. Content Moderation**
- Review and approve/reject flagged quizzes
- Manage reported content
- View quiz statistics and performance
- Monitor quiz usage patterns

### **6. System Maintenance**
- Database backup management
- System health monitoring
- Performance metrics tracking
- Error log monitoring
- Update management

### **7. Configuration**
- Manage system-wide quiz settings
- Configure anti-cheating settings
- Set up role permissions
- Manage notification templates
- Configure email settings

### **8. Analytics & Reporting**
- Generate usage reports
- Export user data
- Quiz performance analytics
- System utilization reports
- User engagement metrics

### **9. Integration Management**
- Configure third-party integrations
- Manage API keys
- Set up external services
- Configure authentication providers

The current implementation provides the core user management and system configuration capabilities. Would you like me to add any of these additional features?